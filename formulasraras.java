/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package kpi;

import java.util.*;

/**
 *
 * @author isft118
 */
public class formulasraras {
    /*public static void main(String[] args){
        float pi = (float) 3.141592;
        System.out.println("Ingrese un valor para X");
        Scanner entrada = new Scanner (System.in);
        float x = entrada.nextFloat();
        System.out.println("Ingrese un valor para Y");
        entrada = new Scanner (System.in);
        float y = entrada.nextFloat();
        System.out.println("Ingrese un valor para Z");
        entrada = new Scanner (System.in);
        float z = entrada.nextFloat();
        entrada.close();
        
        if((x >= 0) && (x <= 1000)){
            if((y >= 0) && (y <= 1000)){
                if((z >= 0) && (z <= 1000)){
                    z *= z;
                    float temp1 = x + (x*y) + (x * z);
                    float temp2 = (x*y) + (x*pi) + (pi*y) + (pi*pi);
                    System.out.println(temp1/temp2);
                }
            }   
        }
    }*/
}